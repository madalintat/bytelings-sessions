"""Rung 4: Solo implement.

Topic: parse a "Last, First" name into "First Last"

Implement `flip_name(s)`:
- Input is "Last, First" (comma may have any whitespace around it).
- Output is "First Last" with single-space separator and each word
  Capitalized.
- If the input doesn't contain a comma, capitalize and return it
  trimmed (treat it as already a first name).
- Empty / whitespace-only input returns "".

The tests in 04_solo_test.py are HIDDEN. Don't peek before you try.
"""


def flip_name(s: str) -> str:
    raise NotImplementedError
