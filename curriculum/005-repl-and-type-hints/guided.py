"""Rung 3 — implement an annotated `clamp`.

Clamp x to be at least `low` and at most `high`.
"""


def clamp(x: float, low: float, high: float) -> float:
    """
    >>> clamp(5, 0, 10)
    5
    >>> clamp(-1, 0, 10)
    0
    >>> clamp(99, 0, 10)
    10
    """
    raise NotImplementedError
