"""Rung 3: Guided implement. Fill in `is_even`.

Topic: function basics + the modulo operator

The signature, docstring, and edge cases are pre-built. You write the body.
"""


def is_even(n: int) -> bool:
    """Return True if `n` is even, False otherwise.

    Notes:
        - 0 is even.
        - Negative numbers can be even too.

    >>> is_even(2)
    True
    >>> is_even(7)
    False
    >>> is_even(0)
    True
    >>> is_even(-4)
    True
    """
    # TODO: one line. Hint: the modulo operator (%) returns the remainder.
    raise NotImplementedError
