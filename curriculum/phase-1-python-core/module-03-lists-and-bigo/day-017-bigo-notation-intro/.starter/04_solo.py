"""Rung 4: Solo implement.

Topic: most-frequent element in O(n)

Implement `most_common(items)`:

- Return the element that appears the most times.
- On a tie, return the one that appeared FIRST in the input.
- Empty list -> raise ValueError.
- Should run in O(n) (one pass, plus dict lookups which are O(1)).

Examples:
    most_common([1, 2, 2, 3, 3, 3])    -> 3
    most_common(['a', 'b', 'a', 'b'])  -> 'a'   # tie, 'a' came first
    most_common([42])                  -> 42

The tests in 04_solo_test.py are HIDDEN. Don't peek before you try.
"""


def most_common(items: list):
    raise NotImplementedError
