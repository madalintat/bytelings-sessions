"""Rung 4: Solo implement.

Topic: build a comma-and-and list of names

Implement `english_list(names)`:

- []                 -> ""
- ["alice"]          -> "alice"
- ["alice", "bob"]   -> "alice and bob"
- ["a", "b", "c"]    -> "a, b, and c"   (Oxford comma)
- ["a","b","c","d"]  -> "a, b, c, and d"

Use ', '.join and str slicing/concat in a way that stays O(n) overall.
Don't build the result with `+=` in a loop.

The tests in 04_solo_test.py are HIDDEN. Don't peek before you try.
"""


def english_list(names: list[str]) -> str:
    raise NotImplementedError
