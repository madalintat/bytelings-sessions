"""Rung 3: Guided implement.

Topic: slicing with start/stop/step

Implement `every_other` using a single slice expression.
"""


def every_other(s: str) -> str:
    """Return every other character of `s`, starting from index 0.

    >>> every_other("abcdef")
    'ace'
    >>> every_other("")
    ''
    >>> every_other("x")
    'x'
    >>> every_other("panopticon")
    'pnpio'
    """
    # TODO: one slice with a step.
    raise NotImplementedError
