"""Rung 4: Solo implement. No scaffold beyond the spec.

Topic: writing a function from scratch with hidden tests

Implement a function `count_vowels` that takes a string and returns
the number of vowels in it.

- Vowels are: a, e, i, o, u (case-insensitive)
- Empty string returns 0
- Non-letter characters don't count

The tests in 04_solo_test.py are HIDDEN. Don't peek before you try.
"""


def count_vowels(text: str) -> int:
    raise NotImplementedError
