"""Rung 3: Guided implement.

Topic: classify pairs of values

Implement `relation(a, b)` returning one of three strings:
    "identical"  — a and b are the same object (a is b)
    "equal"      — a == b but a is not b
    "distinct"   — a != b

Pattern: check `is` first, then `==`, then fall through to "distinct".
"""


def relation(a, b) -> str:
    """Return 'identical', 'equal', or 'distinct'."""
    # TODO: 3 lines, no surprises.
    raise NotImplementedError
