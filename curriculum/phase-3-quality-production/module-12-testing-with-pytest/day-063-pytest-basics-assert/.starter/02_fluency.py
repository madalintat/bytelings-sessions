"""Rung 2: Fluency drill — fix the function so the existing tests pass.

Topic: pytest's assert + pytest.raises + pytest.approx.

The test file already uses the three idioms. Your job is just to make
`area_of_circle` correct so they all pass.
"""
import math


def area_of_circle(radius: float) -> float:
    """Return pi * r^2. Raise ValueError if radius is negative."""
    # TODO: handle negative radius -> ValueError("negative radius")
    # TODO: return the area
    return 0.0
