"""Rung 4: Solo implement.

Topic: write `reverse_string(s)` using a stack.

Using a stack to reverse a string is the canonical "do you understand
LIFO" exercise. Push every character, then pop them all and join.

Spec:
    reverse_string("hello") == "olleh"
    reverse_string("")      == ""
    reverse_string("a")     == "a"

You MUST use a stack-style algorithm (push then pop). Do not use
slicing (s[::-1]) or reversed(). The hidden tests don't enforce that
syntactically, but the point of the exercise is to feel a stack do work.

The tests in 04_solo_test.py are HIDDEN. Don't peek before you try.
"""


def reverse_string(s: str) -> str:
    raise NotImplementedError
