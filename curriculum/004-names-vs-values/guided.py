"""Rung 3 — implement `same_list`.

Return True if two arguments name the SAME list (not just equal lists).
"""


def same_list(a, b) -> bool:
    """
    >>> x = [1, 2]
    >>> same_list(x, x)
    True
    >>> same_list([1, 2], [1, 2])
    False
    """
    # TODO: don't use ==
    raise NotImplementedError
